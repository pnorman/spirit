SELECT
    ST_AsMVTGeom(point, {{unbuffered_bbox}}, {{extent}}, {{buffer}}) AS way,
    names,
    way_area
  FROM leisure
  WHERE point && {{bbox}}
    AND names IS NOT NULL
    AND leisure = 'park'
