SELECT
        ST_AsMVTGeom(geom, {{unbuffered_bbox}}, {{extent}}, {{buffer}}) AS way,
        name,
        name_de,
        name_en,
        kind
    FROM public_transport
    WHERE geom && {{bbox}}
        AND {{zoom}} >= minzoom
