SELECT
        ST_AsMVTGeom(geom, {{unbuffered_bbox}}, {{extent}}, {{buffer}}) AS way,
        name,
        name_de,
        name_en,
        kind,
        motor_vehicle
    FROM ferries
    WHERE geom && {{bbox}}
        AND {{zoom}} >= minzoom
