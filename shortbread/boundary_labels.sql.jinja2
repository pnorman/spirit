SELECT
        ST_AsMVTGeom(geom, {{unbuffered_bbox}}, {{extent}}, {{buffer}}) AS way,
        names,
        admin_level,
        way_area
    FROM boundary_labels
    WHERE geom && {{bbox}}
        AND {{zoom}} >= minzoom
    ORDER BY way_area DESC
